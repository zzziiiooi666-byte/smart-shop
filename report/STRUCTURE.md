# هيكل المشروع المنظم

تم تنظيم ملفات المشروع في مجلدات منفصلة لسهولة الصيانة والتنظيم.

## 📁 الهيكل الجديد

```
smart_markt/
├── index.php                    # نقطة الدخول الرئيسية (يعيد التوجيه إلى pages/index.php)
├── pages/                       # الصفحات الرئيسية
│   ├── index.php               # الصفحة الرئيسية
│   ├── products.php            # صفحة المنتجات
│   ├── product-details.php     # تفاصيل المنتج
│   ├── category.php             # صفحة الفئات
│   ├── cart.php                 # سلة التسوق
│   ├── checkout.php             # إتمام الطلب
│   ├── orders.php               # صفحة الطلبات
│   ├── order-details.php        # تفاصيل الطلب
│   └── track-order.php          # تتبع الطلب
│
├── admin/                       # لوحة التحكم والإدارة
│   ├── admin.php                # لوحة التحكم الرئيسية
│   ├── admin-profile.php        # ملف المدير
│   ├── add-product.php          # إضافة منتج
│   ├── edit-product.php         # تعديل منتج
│   ├── delete_products.php      # حذف منتجات
│   ├── add_products_simple.php   # إضافة منتجات بسيطة
│   ├── add_products_from_images.php  # إضافة منتجات من الصور
│   └── update_products_with_images.php  # تحديث منتجات بالصور
│
├── auth/                        # المصادقة والتسجيل
│   ├── login.php                # تسجيل الدخول
│   ├── register.php             # إنشاء حساب
│   ├── logout.php               # تسجيل الخروج
│   └── check_admin.php          # التحقق من المدير
│
├── user/                        # صفحات المستخدم
│   ├── profile.php              # الملف الشخصي
│   └── notifications.php        # الإشعارات
│
├── tools/                       # أدوات مساعدة
│   ├── diagnose.php             # تشخيص المشاكل
│   ├── fix_admin_password.php   # إصلاح كلمة مرور المدير
│   └── reset_database.php       # إعادة إنشاء قاعدة البيانات
│
├── config/                      # ملفات الإعدادات
│   ├── config.php               # الإعدادات العامة
│   └── database.php             # إعدادات قاعدة البيانات
│
├── includes/                    # ملفات مشتركة
│   ├── header.php               # رأس الصفحة
│   └── footer.php               # تذييل الصفحة
│
├── assets/                      # الملفات الثابتة
│   ├── css/
│   ├── js/
│   └── images/
│
├── api/                         # API Endpoints
│   └── cart/
│
├── database/                    # ملفات قاعدة البيانات
│   └── *.sql
│
├── uploads/                     # الملفات المرفوعة
└── report/                      # التقارير والوثائق
```

## 🔗 المسارات المحدثة

### الصفحات الرئيسية
- `/` → `/pages/index.php`
- `/products.php` → `/pages/products.php`
- `/product-details.php` → `/pages/product-details.php`
- `/category.php` → `/pages/category.php`
- `/cart.php` → `/pages/cart.php`
- `/checkout.php` → `/pages/checkout.php`
- `/orders.php` → `/pages/orders.php`
- `/order-details.php` → `/pages/order-details.php`
- `/track-order.php` → `/pages/track-order.php`

### المصادقة
- `/login.php` → `/auth/login.php`
- `/register.php` → `/auth/register.php`
- `/logout.php` → `/auth/logout.php`

### الإدارة
- `/admin.php` → `/admin/admin.php`
- `/admin-profile.php` → `/admin/admin-profile.php`
- `/add-product.php` → `/admin/add-product.php`
- `/edit-product.php` → `/admin/edit-product.php`

### المستخدم
- `/profile.php` → `/user/profile.php`
- `/notifications.php` → `/user/notifications.php`

## 📝 ملاحظات مهمة

1. **ملف index.php في الجذر**: يعيد التوجيه تلقائياً إلى `/pages/index.php`
2. **جميع المسارات محدثة**: تم تحديث جميع `require_once` و `SITE_URL` في جميع الملفات
3. **ملف config.php**: تم تحديث مسار تسجيل الدخول من `/login.php` إلى `/auth/login.php`
4. **ملف header.php**: تم تحديث جميع الروابط في القائمة الرئيسية

## ✅ المهام المكتملة

- ✅ إنشاء مجلدات التنظيم
- ✅ نقل ملفات الصفحات الرئيسية
- ✅ نقل ملفات الإدارة
- ✅ نقل ملفات المصادقة
- ✅ نقل ملفات المستخدم
- ✅ نقل ملفات الأدوات
- ✅ تحديث جميع المسارات

## 🚀 الاستخدام

بعد التنظيم، يمكنك الوصول إلى الصفحات بنفس الطريقة السابقة، أو استخدام المسارات الجديدة مباشرة.

