# Shop Smart - PHP Version
## دليل التثبيت والتشغيل الكامل

### المتطلبات
- ✅ XAMPP (أو أي خادم محلي يدعم PHP و MySQL)
- ✅ PHP 7.4 أو أحدث
- ✅ MySQL 5.7 أو أحدث
- ✅ متصفح ويب حديث

---

## 🚀 خطوات التثبيت السريع

### 1. تثبيت XAMPP
1. قم بتحميل XAMPP من [xampp.org](https://www.apachefriends.org/)
2. قم بتثبيته
3. افتح XAMPP Control Panel
4. اضغط **Start** على **Apache**
5. اضغط **Start** على **MySQL**

### 2. إعداد قاعدة البيانات

#### الطريقة الأولى: عبر phpMyAdmin
1. افتح المتصفح واذهب إلى: `http://localhost/phpmyadmin`
2. انقر على **New** لإنشاء قاعدة بيانات جديدة
3. اسم القاعدة: `shop_smart`
4. انقر على **Import**
5. اختر ملف `database/schema.sql`
6. انقر **Go**

#### الطريقة الثانية: عبر MySQL Command Line
```sql
-- افتح MySQL Command Line من XAMPP
mysql -u root -p
-- ثم قم بتشغيل محتوى ملف database/schema.sql
```

### 3. إعداد المشروع

#### أ) نسخ الملفات
انسخ مجلد `smart_markt` إلى:
```
C:\xampp\htdocs\smart_markt
```

#### ب) تعديل إعدادات قاعدة البيانات
افتح ملف `smart_markt/config/database.php` وعدّل:
```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');        // اسم المستخدم
define('DB_PASS', '');            // كلمة المرور (افتراضي: فارغة)
define('DB_NAME', 'shop_smart');  // اسم قاعدة البيانات
```

#### ج) تعديل رابط الموقع
افتح ملف `smart_markt/config/config.php` وعدّل:
```php
define('SITE_URL', 'http://localhost/smart_markt');
```
**ملاحظة:** غيّر المسار حسب موقع ملفاتك في `htdocs`

### 4. إنشاء مجلد الرفع (اختياري)
```bash
mkdir smart_markt/uploads
```
أو أنشئ المجلد يدوياً وأعطه صلاحيات الكتابة

---

## 🎯 تشغيل المشروع

1. تأكد من تشغيل **Apache** و **MySQL** في XAMPP
2. افتح المتصفح واذهب إلى:
   ```
   http://localhost/smart_markt
   ```

---

## 👤 الحساب الافتراضي (Admin)

بعد استيراد قاعدة البيانات، يمكنك تسجيل الدخول بـ:
- **البريد:** `admin@shop.com`
- **كلمة المرور:** `Admin@123`

---

## 📁 هيكل المشروع

```
smart_markt/
├── api/                    # API Endpoints
│   └── cart/              # API السلة
│       ├── add.php        # إضافة للسلة
│       ├── update.php     # تحديث الكمية
│       ├── delete.php     # حذف من السلة
│       └── get-count.php  # عدد المنتجات
├── assets/                # الملفات الثابتة
│   ├── css/              # ملفات CSS
│   │   └── style.css     # التصميم الرئيسي
│   ├── js/               # ملفات JavaScript
│   │   └── main.js       # JavaScript الرئيسي
│   └── images/           # الصور
├── config/                # ملفات الإعدادات
│   ├── config.php        # الإعدادات العامة
│   └── database.php      # إعدادات قاعدة البيانات
├── database/              # ملفات قاعدة البيانات
│   └── schema.sql        # مخطط قاعدة البيانات
├── includes/              # ملفات مشتركة
│   ├── header.php        # رأس الصفحة
│   └── footer.php        # تذييل الصفحة
├── index.php             # الصفحة الرئيسية
├── login.php             # تسجيل الدخول
├── register.php          # إنشاء حساب
├── products.php          # صفحة المنتجات
├── product-details.php   # تفاصيل المنتج
├── category.php          # صفحة الفئات
├── cart.php              # سلة التسوق
├── checkout.php          # إتمام الطلب
├── orders.php            # صفحة الطلبات
├── logout.php            # تسجيل الخروج
└── .htaccess             # إعدادات Apache
```

---

## ✨ الميزات

### للمستخدمين:
- ✅ تسجيل الدخول والتسجيل
- ✅ عرض المنتجات
- ✅ البحث والفلترة حسب الفئة
- ✅ عرض تفاصيل المنتج
- ✅ إضافة المنتجات للسلة
- ✅ إدارة السلة (تعديل/حذف الكمية)
- ✅ إتمام الطلب
- ✅ عرض الطلبات السابقة

### للمسؤولين:
- ✅ نظام المصادقة
- ✅ حماية الصفحات
- ✅ جاهز لإضافة لوحة التحكم

---

## 🔧 التقنيات المستخدمة

- **Backend:** PHP 7.4+
- **Database:** MySQL 5.7+
- **Frontend:** HTML5 + CSS3 + JavaScript
- **Server:** Apache (XAMPP)
- **Icons:** Font Awesome 6.4.0

---

## 🐛 استكشاف الأخطاء

### خطأ في الاتصال بقاعدة البيانات
- ✅ تأكد من تشغيل MySQL في XAMPP
- ✅ تحقق من إعدادات `config/database.php`
- ✅ تأكد من وجود قاعدة البيانات `shop_smart`

### الصور لا تظهر
- ✅ تأكد من وجود مجلد `uploads` مع صلاحيات الكتابة
- ✅ تحقق من مسار الصور في قاعدة البيانات
- ✅ استخدم روابط خارجية للصور (مثل Unsplash)

### الصفحة لا تعمل
- ✅ تأكد من تشغيل Apache في XAMPP
- ✅ تحقق من مسار الملفات في `config/config.php`
- ✅ راجع ملف `error_log` في XAMPP
- ✅ تأكد من أن PHP يعمل: افتح `http://localhost/phpinfo.php`

### خطأ 404
- ✅ تحقق من مسار الملفات
- ✅ تأكد من أن `.htaccess` موجود
- ✅ تحقق من إعدادات Apache

---

## 📝 ملاحظات مهمة

### الأمان:
⚠️ هذا المشروع للتعلم والتطوير فقط. في الإنتاج:
- استخدم HTTPS
- أضف CSRF Protection
- قم بتحسين Input Validation
- استخدم Environment Variables للأسرار

### الأداء:
- يمكن إضافة Caching
- تحسين استعلامات قاعدة البيانات
- ضغط الملفات الثابتة

---

## 📚 الخطوات التالية (اختيارية)

يمكنك إضافة:
1. لوحة تحكم للمسؤولين
2. نظام رفع الصور
3. دفع إلكتروني (Razorpay/PayPal)
4. نظام الإشعارات
5. تقييمات المنتجات
6. نظام الكوبونات
7. تقارير المبيعات

---

## 💡 نصائح

- استخدم حساب Admin لإضافة منتجات جديدة
- يمكنك تعديل التصميم في `assets/css/style.css`
- يمكنك إضافة وظائف JavaScript في `assets/js/main.js`
- جميع الاستعلامات تستخدم Prepared Statements للأمان

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع ملف `error_log` في XAMPP
2. تأكد من إعدادات PHP (افتح `phpinfo()`)
3. تحقق من صلاحيات الملفات والمجلدات
4. تأكد من إصدار PHP و MySQL

---

**تم التطوير بـ ❤️ باستخدام PHP + MySQL + HTML + CSS**

**الإصدار:** 1.0.0  
**تاريخ التحديث:** 2024

