# 🚀 دليل رفع المشروع على GitHub

## الخطوات

### 1. إنشاء مستودع جديد على GitHub

1. اذهب إلى [GitHub](https://github.com)
2. انقر على زر **"New"** أو **"+"** في أعلى الصفحة
3. اختر **"New repository"**
4. املأ التفاصيل:
   - **Repository name**: `smart_markt` (أو أي اسم تريده)
   - **Description**: `نظام إدارة المتاجر الإلكترونية - Shop Smart E-commerce System`
   - اختر **Public** (أو Private حسب رغبتك)
   - **لا** تضع علامة على "Initialize with README" (لأننا أنشأنا README.md بالفعل)
5. انقر على **"Create repository"**

### 2. ربط المشروع المحلي بـ GitHub

بعد إنشاء المستودع على GitHub، ستظهر لك تعليمات. استخدم الأوامر التالية:

```bash
# إضافة المستودع البعيد (استبدل YOUR_USERNAME باسمك على GitHub)
git remote add origin https://github.com/YOUR_USERNAME/smart_markt.git

# تغيير اسم الفرع الرئيسي إلى main (إذا لزم الأمر)
git branch -M main

# رفع المشروع إلى GitHub
git push -u origin main
```

### 3. إذا طُلب منك تسجيل الدخول

إذا طُلب منك اسم المستخدم وكلمة المرور:
- **Username**: اسم المستخدم على GitHub
- **Password**: استخدم **Personal Access Token** بدلاً من كلمة المرور

#### كيفية إنشاء Personal Access Token:

1. اذهب إلى GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. انقر على **"Generate new token"**
3. اختر الصلاحيات: `repo` (كل الصلاحيات)
4. انسخ الرمز المولّد واستخدمه ككلمة مرور

### 4. التحقق من الرفع

بعد الرفع، اذهب إلى صفحة المستودع على GitHub:
```
https://github.com/YOUR_USERNAME/smart_markt
```

ستجد جميع الملفات موجودة!

## 📝 ملاحظات مهمة

### قبل الرفع:

1. **تحقق من `.gitignore`**: تأكد من أن الملفات الحساسة غير مرفوعة
   - ملفات الإعدادات المحلية
   - كلمات المرور
   - الملفات المؤقتة

2. **تحديث `README.md`**: 
   - استبدل `YOUR_USERNAME` باسمك على GitHub
   - أضف أي معلومات إضافية

3. **تحديث `config/config.php`**:
   - لا ترفع ملفات الإعدادات الحساسة
   - استخدم `.env` أو ملفات إعدادات منفصلة

### بعد الرفع:

1. **إضافة وصف للمستودع** على GitHub
2. **إضافة Topics** (مثل: php, ecommerce, arabic, shopping)
3. **إضافة License** (MIT)
4. **إنشاء Release** للإصدار الأول

## 🔄 التحديثات المستقبلية

عند إجراء تغييرات:

```bash
# إضافة التغييرات
git add .

# إنشاء commit
git commit -m "وصف التغييرات"

# رفع التغييرات
git push
```

## 👥 مشاركة المشروع مع الفريق

### للمطورين الآخرين:

```bash
# Clone المشروع
git clone https://github.com/YOUR_USERNAME/smart_markt.git

# الانتقال إلى المجلد
cd smart_markt

# إعداد قاعدة البيانات (راجع SETUP.md)
```

## 📚 روابط مفيدة

- [GitHub Docs](https://docs.github.com)
- [Git Basics](https://git-scm.com/book)
- [GitHub Desktop](https://desktop.github.com) (واجهة رسومية)

---

**نصيحة:** استخدم GitHub Desktop إذا كنت تفضل واجهة رسومية بدلاً من سطر الأوامر!

